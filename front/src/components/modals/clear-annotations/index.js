import React, { Component } from 'react';
import styles from './ClearAnnotations.scss';

class ClearAnnotations extends Component {
  render() {
    return (
      <section className={styles.clearanno}>
        <div></div>
      </section>
    );
  }
}

export default ClearAnnotations;
