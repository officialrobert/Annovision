import React from 'react';
import styles from './RemoveFile.scss';

const RemoveFile = (props) => {
  return (
    <section className={styles.removefile}>
      <div></div>
    </section>
  );
};

export default RemoveFile;
